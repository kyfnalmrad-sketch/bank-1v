# النسخ الاحتياطي وتعليمات الوصول المؤجلة

## الوضع الحالي

تم تأجيل النسخة الاحتياطية المشفرة بطلب المستخدم حتى لا تعطل تشغيل نسخة Render أو إصلاحاتها. يوجد سر عبارة التشفير في GitHub، لكن تشغيل النسخ الاحتياطي لم يكتمل لأن قيمة `RENDER_POSTGRES_URL` الحالية لا تُفسَّر كرابط Postgres خارجي صالح في GitHub Actions.

## الإجراء المطلوب عند استئناف النسخ الاحتياطي

1. من Render، افتح قاعدة Postgres الخاصة بالمشروع ثم انسخ **External Database URL** الكامل الذي يبدأ بـ`postgresql://` أو `postgres://`.
2. من GitHub، افتح المستودع ثم **Settings → Secrets and variables → Actions**.
3. أنشئ أو حدّث السر `RENDER_POSTGRES_URL` بالرابط كاملًا، من دون علامات اقتباس أو مسافات زائدة.
4. اترك `BACKUP_ENCRYPTION_PASSPHRASE` كما هو؛ لا ترسل أيًا من القيمتين عبر الدردشة.
5. شغّل سير العمل `render-postgres-backup.yml` ثم تحقق من نجاح التصدير والملف المشفر والبصمة.

## تعليمات الوصول

لم يجر أي تغيير في صلاحيات المستخدمين أو صفحات الدخول. أي تعليمات وصول جديدة، أو أدوار مستخدمين، أو تعديل مصادقة، تبقى مؤجلة حتى يحدد المستخدم المتطلبات كتابةً. لا يتضمن هذا المشروع في وضع Staging تخزينًا محليًا لبيانات اعتماد Render أو GitHub.
