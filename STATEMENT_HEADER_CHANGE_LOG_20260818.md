# سجل فروقات ترويسة كشف الحساب

**المرجع:** `prototype_0_5/templates/kuraimi.js` من Prototype 0.5.1 المحفوظ.  
**النطاق:** معاينة كشف الحساب في Web Staging فقط. لا يتناول هذا السجل Prototype 0.5.1 ولا يعدله.

| الموضع | القالب المرجعي | الوضع الذي ظهر في Web Staging | الحالة المطلوبة |
|---|---|---|---|
| العمود الأيمن في الترويسة، السطر الثاني | `Account Currency: USD` | `Period: 04/02/2026 to 04/08/2026` | **يُزال Period** ويُستعاد `Account Currency` في نفس الموضع |
| العمود الأيمن في الترويسة، السطر الثالث | `Date: [تاريخ نهاية الكشف]` | `Statement Ref: [مرجع]` | **يُزال Statement Ref** ويُستعاد `Date` في نفس الموضع |
| عداد الصفحة | عداد تقني مخفي بصريًا في القالب المرجعي | `PAGE 1/…` ظاهر في الترويسة | **يُخفى**؛ لا يظهر في ترويسة الكشف |
| عنوان الترويسة | يعتمد القالب على رسم الترويسة المرجعي والبيانات الوصفية فقط | أضيف عنوان نصي ظاهر `STATEMENT` في معاينة Staging | يُراجع ويُزال في الاستعادة لتبقى الترويسة كما المرجع |

## عناصر لم تتغير ضمن هذا الطلب

يستمر صف البيانات الأيسر في استخدام الحقول المرجعية: `Customer Name` و`Account Number` و`Momaiz No.`، ويستمر الصف الأول في العمود الأيمن في استخدام `Branch Name`. كما تبقى أصول الترويسة والـQR والجداول والتذييل من مسارات Prototype 0.5.1 الأصلية.

> لا يُنفّذ أي تغيير إضافي على كشف الحساب خارج عناصر الاستعادة المذكورة إلا بعد موافقة صريحة من المستخدم.

## نتيجة الاستعادة

في 18 أغسطس 2026 أزيلت من ترويسة Staging العناصر غير المرجعية الثلاثة: `Period` و`Statement Ref` والعنوان النصي `STATEMENT`، كما أزيل عداد الصفحة المرئي. أصبحت ترويسة الكشف تعرض بالترتيب المرجعي: `Branch Name` ثم `Account Currency` ثم `Date`. تم التحقق منها بصريًا في المعاينة المحلية وباختبار يمنع عودة الحقول المحذوفة.
