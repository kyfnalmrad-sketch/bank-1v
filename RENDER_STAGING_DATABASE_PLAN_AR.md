# قاعدة بيانات Render لبيئة Staging

## النطاق

قاعدة البيانات `bank-karimi-web-staging-db` هي قاعدة **PostgreSQL مجانية** لنسخة Staging فقط. تستخدم للاختبار الداخلي للواجهة والاستيراد والمراجعة، ولا تعد بديلًا عن بيئة تشغيل دائمة أو مستودع لبيانات العملاء الحقيقية. تبقى نسخة Prototype 0.5.1 مرجعًا محفوظًا خارج هذه القاعدة.

| العنصر | الإعداد المعتمد |
|---|---|
| الخطة | Render Postgres Free |
| السعة | 1GB |
| الذاكرة | 256MB |
| المعالج | 0.1 CPU |
| المنطقة | Ohio (US East) |
| التخزين التلقائي | معطل |
| التوفر العالي | معطل |
| الدومين | غير مطلوب لبيئة Staging |

> تظهر لوحة Render أن القاعدة المجانية الحالية تنتهي في **17 سبتمبر 2026** إذا لم تُرقَّ الخطة؛ لذلك تكون النسخ الاحتياطية خارج القاعدة جزءًا إلزاميًا من مرحلة التسليم.

## الجداول المقترحة

| الجدول | الغرض | بيانات لا تخزن فيه |
|---|---|---|
| `staging_profiles` | بيانات العميل والحساب اللازمة لجلسة المعاينة | ملفات Excel أو PDF الخام |
| `staging_statements` | رأس الكشف وحالة المراجعة والملخصات | كلمات المرور أو بيانات الاتصال |
| `staging_transactions` | الحركات المقبولة والمرفوضة والمرجع الداخلي والخارجي | ملفات أو مفاتيح |
| `staging_imports` | اسم الملف وحالة التحليل وخريطة الأعمدة ووقت الاستيراد | محتوى ملف Excel نفسه |
| `staging_description_memory` | وصف مقبول وعدد استخدامه وتواريخه | الوصف المرفوض بوصفه بديلًا تلقائيًا |
| `staging_person_name_memory` | أسماء مستخرجة من أوصاف مقبولة وتواريخها | أي بيانات اعتماد |
| `staging_audit_log` | أحداث الاستيراد والمراجعة والتصدير | قيم حساسة أو أسرار |

## ضوابط الاستخدام

يحتفظ الخادم برابط اتصال Render في متغير سري فقط. لا يظهر في الواجهة ولا ملفات GitHub. لا يسمح مسار الواجهة بحفظ أي استيراد قبل إظهار خريطة الأعمدة والصفوف المرفوضة، ولا يستبدل وصفًا مرفوضًا تلقائيًا. تحفظ البيانات التجريبية فقط، ويصدر النسخ الاحتياطي كملف JSON أو CSV خارجي قبل انتهاء صلاحية القاعدة.
